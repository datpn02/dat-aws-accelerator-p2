# W8 - Day A: Terraform Foundation

**Date:** 01/06/2026

## Objectives

* Hiểu khái niệm Infrastructure as Code (IaC).
* Làm quen với Terraform và ngôn ngữ HCL.
* Nắm được Terraform workflow.


---

## 1. Infrastructure as Code (IaC)

### What is IaC?

Infrastructure as Code (IaC) là phương pháp quản lý và provision hạ tầng thông qua code thay vì thao tác thủ công trên giao diện web hoặc CLI.

### Benefits

* Tự động hóa việc tạo hạ tầng.
* Đảm bảo tính nhất quán giữa các môi trường.
* Dễ dàng version control bằng Git.
* Hỗ trợ collaboration giữa các thành viên.
* Dễ rollback khi có sự cố.

### Traditional vs IaC

| Traditional     | IaC                |
| --------------- | ------------------ |
| Manual ClickOps | Code-based         |
| Khó tái sử dụng | Reusable           |
| Dễ sai sót      | Consistent         |
| Khó audit       | Version controlled |

---

## 2. Terraform Overview

Terraform là công cụ IaC do HashiCorp phát triển, sử dụng declarative configuration để quản lý hạ tầng trên nhiều nền tảng:

* AWS
* Azure
* GCP
* Kubernetes
* GitHub
* Docker

### Terraform Architecture

```text
Terraform Configuration
        ↓
Terraform Core
        ↓
Provider (AWS/K8s/etc.)
        ↓
Infrastructure
```

---

## 3. HCL Syntax

Terraform sử dụng HashiCorp Configuration Language (HCL).

### Basic Example

```hcl
provider "aws" {
  region = "ap-southeast-1"
}

resource "aws_s3_bucket" "demo" {
  bucket = "my-demo-bucket"
}
```

### Common Blocks

#### Provider

```hcl
provider "aws" {
  region = "ap-southeast-1"
}
```

#### Resource

```hcl
resource "aws_instance" "web" {
  ami           = "ami-xxxxxxxx"
  instance_type = "t2.micro"
}
```

#### Variable

```hcl
variable "instance_type" {
  default = "t2.micro"
}
```

#### Output

```hcl
output "instance_id" {
  value = aws_instance.web.id
}
```

---

## 4. Terraform Workflow

### terraform init

Khởi tạo project Terraform.

```bash
terraform init
```

Chức năng:

* Download providers.
* Khởi tạo backend.
* Chuẩn bị working directory.

### terraform plan

Xem trước thay đổi.

```bash
terraform plan
```

### terraform apply

Áp dụng thay đổi lên hạ tầng.

```bash
terraform apply
```

### terraform destroy

Xóa toàn bộ tài nguyên được quản lý.

```bash
terraform destroy
```

## Key Learnings

* IaC giúp quản lý hạ tầng bằng code.
* Terraform là công cụ IaC phổ biến nhất hiện nay.
* HCL là ngôn ngữ cấu hình của Terraform.
* Workflow cơ bản gồm: Init → Plan → Apply → Destroy.

